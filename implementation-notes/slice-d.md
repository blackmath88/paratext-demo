# Slice D — inversion and reframing

1. **Persistent scene objects:** the same 16 operation groups, conversation and
   artifact furniture, persistent artifact text, latent cluster/relationship
   geometry, four projection targets and a pre-existing HTML selector.
2. **Object transformations:** chronology and artifact collapse into stable
   operations; supersession becomes an explicit relation; operations cluster by
   context; four projection ports emerge; the same nodes travel through Essay,
   Thread, Structure and Spec arrangements before settling.
3. **New elements:** context cluster labels, supersession/source links,
   projection ports, current-frame label and four accessible selector buttons.
4. **Timeline range:** Acts 00–07 occupy 0–.84; Act 08 owns .84–1.00.
5. **Reduced-motion endpoint:** a meaningful Essay projection; selector changes
   apply immediately while retaining operation identity.
6. **Likely performance concern:** repeated transforms of 16 groups. Layouts are
   precomputed and require no DOM measurement.
7. **Visual claim:** neither conversation nor document is canonical. Durable
   operations are primary; each coherent unity is a temporary projection.
