# Slice D — Open

- **Persistent objects:** the full 16-operation field at the Cost endpoint, its
  shared `operationStates`, all four projection layouts, and the live frame
  switcher.
- **Transformations:** Cost annotations and vendor-selected framing recede;
  operation nodes do not move, disappear or resolve into a privileged final
  workspace. The endpoint question appears over the still-visible field.
- **New elements:** one latent scene-owned text group containing the exact
  question `Which frame now?`. No closing card and no replacement scene.
- **Timeline range:** `.92–1`, settle at the 70% choreography boundary. The
  `open` entry moves from `plannedActs` into `acts`.
- **Reduced motion endpoint:** the same stable question and visible Spec field,
  with the switcher live and all four projections immediately inspectable.
- **Performance concern:** no operation tween and one short text reveal; live
  switching continues to use the bounded shared-layout writer.
- **Visual claim:** the material remains available, but no single view closes
  the argument. The viewer answers the final question by choosing a frame.
