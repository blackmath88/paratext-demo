# Slice A — Recovery

- **Persistent objects:** all 16 durable operation groups and their
  `operationStates`; the existing computational field and tube endpoint;
  `superseded-history` and `decision-reason` remain the same semantic nodes.
- **Transformations:** the pulled-back stream resolves into a bounded stretch;
  tool operations move to a subordinate apparatus column; stable identifiers
  appear beside every operation; the superseded claim is struck and connected
  to the decision that replaces it. Operation travel uses `tweenOperation()`.
- **New elements:** one latent scene-owned Recovery furniture group containing
  the segment boundary/title/rule, apparatus heading and inherited footnote-like
  rule, identifier labels, editorial strike, and supersession relation.
- **Timeline range:** `.68–.76`, settle at the 70% choreography boundary. The
  `recovery` entry moves from `plannedActs` into `acts`.
- **Reduced motion endpoint:** the same timeline is seeked to the completed
  Recovery plate: bounded segment, subordinate tools, stable IDs and legible
  supersession relation.
- **Performance concern:** reuse 16 operation nodes and 16 short identifier
  labels; no layout measurement, duplicated operation, or bulk transcript.
- **Visual claim:** chat is rediscovering the devices Part I accumulated—rule,
  margin, folio and strike—without changing back into paper or historical
  costume.
