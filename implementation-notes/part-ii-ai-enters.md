# Part II — AI enters

- **Persistent objects:** six fragment frame states, the shared software
  surface, all 16 semantic operation nodes, a latent AI viewport/tube and the
  scene-owned material state.
- **Transformation:** specialized frames converge on one conversation surface;
  four exchanges establish the useful language-first state; the remaining
  operations accumulate chronologically; the camera pulls back to expose the
  stream beyond its viewport.
- **New elements:** screen grid, AI surface/header/input/scroll indicator,
  clipping viewport, long computational strip and continuation markers.
- **Timeline:** after the Part II rebalance, Part I ends with Fragments at .52.
  `II·01` AI / Conversation is .52–.60; `II·02` The Tube is .60–.68. Both
  retain master-managed plateaus; Recovery now occupies .68–.76 and .76–1 is
  reserved for the three later acts.
- **Reduced motion:** section seeking lands on the calm four-turn conversation
  and the fully revealed tube endpoints.
- **Performance:** 16 existing operation groups imply scale; no bulk message
  generation, layout measurement or new dependency.
- **Visual claim:** AI's radical intelligence initially benefits from a simple
  language interface; as chronology grows, the viewport becomes a small window
  onto a structurally ancient scroll.
